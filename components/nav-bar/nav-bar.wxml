<view class="nav-bar" style="height: {{statusBarHeight + navBarHeight}}px; padding-top: {{statusBarHeight}}px;">
  <view class="nav-bar__left" bindtap="onHomeTap">
    <view class="nav-bar__home">
      <view class="home-icon"></view>
    </view>
  </view>
  <view class="nav-bar__title" wx:if="{{title}}">{{title}}</view>
  <view class="nav-bar__right">
    <!-- 右侧胶囊按钮区域 -->
  </view>
</view>
